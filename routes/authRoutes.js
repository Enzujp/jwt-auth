const { Router } = require("express");
const router = Router();

router.get('/signup', ()=>{});
router.post('/signup', ()=>{});

router.get('/signin', ()=>{});
router.post('/signin', ()=>{});


module.exports = router;